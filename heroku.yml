setup:
  addons:
    - plan: heroku-postgresql
      as: postgresql-shaped-67538
build:
  docker:
    web: Dockerfile
release:
  command:
    - npm install
  image: web
run:
  web: docker-compose up
  web: npm start
